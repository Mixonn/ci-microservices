spring:
  cloud:
    bus:
      enabled: false
    consul:
      enabled: false
      config:
        enabled: false
      discovery:
        enabled: false
  sleuth:
    annotation:
      enabled: false
  zipkin:
    enabled: false

management:
  metrics:
    export:
      graphite:
        enabled: false # Whether exporting of metrics to Graphite is enabled.

stubrunner:
  ids-to-service-ids:
    deploy-runner: 'app-deploy-runner'
